import { NextResponse } from 'next/server'; import { ObjectId } from 'mongodb'; import { getDb } from '@/lib/mongodb'; import { isAdmin } from '@/lib/auth'; import { signatureStorage } from '@/lib/storage';
export const runtime='nodejs';
export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){ if(!(await isAdmin())) return NextResponse.json({error:'Unauthorized'},{status:401}); const {id}=await params; if(!ObjectId.isValid(id)) return NextResponse.json({error:'ID tidak valid.'},{status:400}); const doc=await (await getDb()).collection('santri').findOne({_id:new ObjectId(id)}); if(!doc) return NextResponse.json({error:'Data tidak ditemukan.'},{status:404}); return NextResponse.json({...doc,_id:doc._id.toHexString()}); }
export async function DELETE(_req:Request,{params}:{params:Promise<{id:string}>}){ if(!(await isAdmin())) return NextResponse.json({error:'Unauthorized'},{status:401}); const {id}=await params; if(!ObjectId.isValid(id)) return NextResponse.json({error:'ID tidak valid.'},{status:400}); const db=await getDb(); const doc=await db.collection('santri').findOne({_id:new ObjectId(id)}); if(!doc) return NextResponse.json({error:'Data tidak ditemukan.'},{status:404}); try { await signatureStorage.delete(doc.signature.url); } catch { return NextResponse.json({error:'Tanda tangan gagal dihapus dari storage. Data tidak dihapus.'},{status:502}); } await db.collection('santri').deleteOne({_id:doc._id}); return NextResponse.json({success:true}); }
