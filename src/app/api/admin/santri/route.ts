import { NextResponse } from 'next/server'; import { getDb } from '@/lib/mongodb'; import { isAdmin } from '@/lib/auth';
export const runtime='nodejs';
export async function GET(req:Request){ if(!(await isAdmin())) return NextResponse.json({error:'Unauthorized'},{status:401}); const {searchParams}=new URL(req.url); const q=(searchParams.get('q')||'').trim(); const page=Math.max(1,Number(searchParams.get('page')||1)); const limit=Math.min(50,Math.max(1,Number(searchParams.get('limit')||20))); const filter=q?{nama:{$regex:q.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'),$options:'i'}}:{}; const db=await getDb(); const [items,total]=await Promise.all([db.collection('santri').find(filter,{projection:{signature:0}}).sort({createdAt:-1}).skip((page-1)*limit).limit(limit).toArray(),db.collection('santri').countDocuments(filter)]); return NextResponse.json({items, total, page, limit, pages:Math.ceil(total/limit)}); }
