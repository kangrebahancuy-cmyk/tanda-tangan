import { getDb } from './mongodb';
export async function rateLimit(scope:string, identifier:string, max:number, windowMs:number){ const db=await getDb(); const now=new Date(); const key=`${scope}:${identifier}`; const c=db.collection('rate_limits'); await c.createIndex({expiresAt:1},{expireAfterSeconds:0}); const result=await c.findOneAndUpdate({key,expiresAt:{$gt:now}},{$inc:{count:1},$setOnInsert:{windowStart:now,expiresAt:new Date(now.getTime()+windowMs)}},{upsert:true,returnDocument:'after'}); const count=result?.count??1; return {allowed:count<=max,remaining:Math.max(0,max-count)}; }
