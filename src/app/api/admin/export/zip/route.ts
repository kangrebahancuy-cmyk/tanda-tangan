import { NextResponse } from 'next/server'; import { isAdmin } from '@/lib/auth'; import { getDb } from '@/lib/mongodb'; import { signatureStorage } from '@/lib/storage'; import archiver from 'archiver'; import { PassThrough } from 'node:stream';
export const runtime='nodejs';
function filename(name:string){return name.normalize('NFKD').replace(/[^a-zA-Z0-9._-]+/g,'-').replace(/-+/g,'-').replace(/^-|-$/g,'').toLowerCase()||'santri';}
export async function GET(){if(!(await isAdmin()))return NextResponse.json({error:'Unauthorized'},{status:401});const docs=await (await getDb()).collection('santri').find({}, {projection:{nama:1,signature:1}}).sort({createdAt:-1}).toArray();const archive=archiver('zip',{zlib:{level:6}});const pass=new PassThrough();archive.pipe(pass);for(const doc of docs){try{const data=await signatureStorage.get(doc.signature.url);archive.append(data,{name:`${filename(doc.nama)}-${doc._id.toHexString().slice(-6)}.png`});}catch(error){console.error('zip signature error',doc._id,error);}}await archive.finalize();const stream=new ReadableStream({start(controller){pass.on('data',c=>controller.enqueue(new Uint8Array(c)));pass.on('end',()=>controller.close());pass.on('error',e=>controller.error(e));}});return new Response(stream,{headers:{'Content-Type':'application/zip','Content-Disposition':'attachment; filename="tanda-tangan-santri.zip"','Cache-Control':'private, no-store'}});}
